# 2026-09-07

- 根 README.md 从空壳重写为完整说明（三端速览、快速开始、配置、架构、权限模型、新手指南 A~E、常用命令、踩坑）。按用户反馈去掉本机环境绑定（phpStudy 路径），文档保持通用。
- 新增根目录 Makefile（monorepo 统一入口）：help/help-en、init、proto/proto-all/proto-ts、dev*/build* 系列，后端用 `$(MAKE) -C admin-backend` 转发；新增 `make dev`（双端并发）、`make build-backend-windows|linux`、`make proto-ts`（ts-proto 生成双端 TS 契端到 src/proto/）。
- make 中文乱码：Git Bash 下 recipe shell 是 /usr/bin/sh（不是 cmd），判断 cmd 必须用 `$(filter Windows_NT,$(shell echo %OS%))`；`$(SHELL)` 不可信（默认 sh.exe）。中文仍乱是终端按 GBK 渲染 UTF-8，故加 `make help-en` 纯 ASCII 兜底。
- git 忽略 node_modules：admin-react 原本没有 .gitignore，其 node_modules(21040)+dist 已入库；新建 admin-react/.gitignore、根 .gitignore 追加 Node 段、`git rm -r --cached` 移出索引（本地保留）。
- 注意：本次改动曾在工作区被回滚一次（.gitignore 消失、索引恢复），已重做；**尚未 commit**，需用户提交才固化。
- 另：`.codebuddy/` 目录本轮被外部删除/清空过，MEMORY.md 与 daily 已重建。
