# 2026-09-05

## 个人中心菜单不显示（排查 + 修复）
- 现象：用户在 DB 加了菜单 id=26 `/account` 个人中心（key=Account，已启用、未隐藏，权限点 AccountView/AccountEdit 已建），前端路由与 `views/account/index.vue` 都有，但侧边栏不显示。
- 根因：前端侧边栏以 `src/router/menu.ts` 的 `localMenuData` 为骨架（`HandleRemoteMenuIntoLocal` 只用后端 menus 覆盖 hideInMenu/icon），**本地表里没有 Account 这个 key → 不渲染**。后端无问题（超管走 FindAdministerPermissions 全量，pageIds 含 26）。
- 修复：双端 `localMenuData` 加 `{ key:'Account', path:'/account', name:'个人中心', icon:'UserOutlined' }`；给角色 1 补绑权限点 33/34（原本只有 23 条，现 25 条）；双端 mock `menuTreeData` 加 Account 菜单 + `permissionData` 加 id33/34 两条。
- 遗留待确认：① docs/wei.sql 是否补这 3 行（菜单 26 + 权限点 33/34）② 受限角色 5（"查看"，现绑 7 条）要不要也给个人中心权限。
- 双端 vue-tsc / tsc 0 错误，改动已生效（HMR）。

## 后续两项调整（已完成）
1. **菜单顺序**：个人中心移到首页之后（系统设置之前）。双端 `localMenuData` 数组顺序即侧边栏顺序，首页由 `SiderMenu` 硬编码置顶（`[homeItem, ...HandleRemoteMenuIntoLocal(...)]`），所以把 Account 放在 `localMenuData` 第一位即可。
2. **个人中心头像非必填**：前端本来就无校验，**元凶是后端** `ReqAccountEdit.avatar` 的 `binding:"url"` —— go-playground 的 `url` 对空字符串会报错。改为 `omitempty,url`（与 email 同构）；proto + pb.go 同步改（按项目约定手工改 pb.go struct tag，不重新生成）。logic 里 `data.Avatar = params.Avatar` 直接赋值，空值可正常保存。
- 验证（tmpcheck4，跑完删）：空头像可保存 / 非法 URL 仍拦截 / 合法头像+邮箱通过 / 昵称仍必填，全 PASS；后端已重编译重启 healthz 200；双端类型检查 0 错误。
- 该接口 `/admin/account/edit` 只被个人中心使用（账号管理走另一个 message），所以改动不影响账号管理页。

## 前端表单未校验排查（2026-09-05）
- 现象：个人中心「编辑资料」「修改密码」必填项为空也直接提交到后端。根因：vue3x 两个组件只写了 `:rules` 但**没有 `ref="formRef"`，提交时也没 `formRef.value?.validate()`**，rules 形同虚设。
- 已修：两个组件加 `ref="formRef"` + `onSave` 里先 `await formRef.value?.validate()`（校验失败直接 return，不发请求）；修改密码补了「新密码变化后重新校验确认密码」（对应 react 版的 `dependencies`）。
- **全量排查结论**：vue3x 共 12 处带 rules 的表单，账号/角色/菜单/接口 的 Add/Edit/ResetPassword（8 处）都有 `formRef.value?.validate()`，登录页用 `@finish`（antd 自动校验），**只有个人中心两处缺失**；react 端 11 处含 rules 的表单全部有 `validateFields()` 或用 `onFinish`，无问题。
- 顺手清理：`views/admin/permission/components/common.ts` 的 `DEFAULT_RULES` / `PERMIDDION_RULES` 及 `AdminPerssionKey` import（随 Add/EditPermissionModal 一起已成死代码，双端删除）。
- ✅ **已用 Playwright 跑通端到端验证（13 项全 PASS）**：mock 服务（8010）+ 本机 Chrome（executablePath 绕过浏览器下载失败）。覆盖场景：编辑资料昵称留空/非法邮箱被拦下且不发请求、头像留空仍可提交、修改密码全部留空被拦下（3 个提示）且不发请求、两次密码不一致被拦下、改一致后错误自动消除、合法填写正常提交。
- 环境要点：playwright 浏览器下不了（Node 不走 VPN 代理 + 镜像 404）→ 用本机 Chrome；脚本放 admin-vue3x 目录；`chcp 65001` 防中文乱码；断言实时 console.log。

## 上传功能模块（完整实现，2026-09-05）
- 需求：后端上传支持本地+阿里云/腾讯云/亚马逊云（可扩展）、默认本地、upload 目录、自定义目录结构；DB 记录链接/原始名/新名/日期；管理后台"上传管理"列表页；上传必填分组（=路径前缀）；支持删除；域名前缀可配。
- 实现：pkg/storage（Driver+Register，4 个驱动）+ config.yaml upload 段 + admin_upload 表 + dao/logic/controller/router + 静态服务 + 6 个错误码(900001-900006) + 权限闭环 + 双端前端页面 + mock。
- 环境结论：**protoc 无原生二进制**（只有 npm grpc-tools 的 protoc.js，GitHub 下载超时）→ 上传模块改用 `app/admin/types` 的 Go struct 定义契约（前端无感知）。**Go 模块可下载**（goproxy.cn）：aliyun-oss / cos-go-sdk-v5 / aws-sdk-go-v2 均装成功。
- 验证：后端 tmpcheck5 7 项全 PASS（上传/目录结构/落盘/分组筛选/非法分组拒绝/类型拒绝/删除清理）；前端 Playwright+mock+本机 Chrome 9 项全 PASS（菜单可见/页面打开/未填分组前端拦截且不发请求/上传成功/列表出现记录含分组前缀/删除生效）。
- 踩坑：① mock 插件只做 JSON.parse，不支持 multipart → 扩展 `MockRequest.rawBody` 并在 mock upload 里手工解析 multipart ② 新增菜单/权限点后 **mock 的 menuTreeData 与 permissionData 也要同步**，否则侧边栏不显示、按钮被 Authorization 隐藏 ③ 分组校验要先验原始输入再 path.Clean。
- 遗留清理：工作区根目录 uv*.log / pwi.log / mock8010.log / genrun.log 与 8010 端口的 mock 服务（删除命令审批超时未执行）。

## 个人中心头像点击上传（双端，已完成 2026-09-05）
- `views/account/components/CurrentAccount.vue` 与 react `CurrentAccount.tsx`：头像输入框 `addonAfter` 内放 Upload「点击上传」按钮；`before-upload` **同步返回 false**（不触发 antd 自动上传），选图后校验：类型仅图片（ext 白名单 jpg/jpeg/png/gif/webp + `image/` mime）、大小 ≤10MB（与后端 config.yaml upload.max_size 一致），通过后调 `uploadAdminFile({ file, uploadGroup: '/account/' })`，成功回填 `formState.avatar` / `form.setFieldValue('avatar', url)`
- 分组固定 `/account/`（个人中心菜单路径前缀）；mock 无需改动（mock upload 支持任意分组，返回相对 url）
- react 左侧大头像预览用 `Form.useWatch('avatar', form)`（vue3x 侧 formState 天然联动）
- vite 双端 `server.proxy` 新增 `/upload` → `http://127.0.0.1:3000`（不 rewrite）：本地存储返回 `/upload/...` 相对路径，需经前端端口显示（后端 Static 免鉴权）。已验证两端 GET `/upload/account/2026/09/x.png` → 200 image/png
- proto 生成流程结论修正：见 MEMORY.md「proto 修改流程」（protoc + protoc-go-inject-tag 可安全重生成）

## 上传默认不限制文件类型（2026-09-05）
- 需求：默认不限制上传文件类型。改动：`config.yaml` 与 `docs/config.yaml` 的 `upload.allowed_exts` 白名单（jpg…zip 11 项）改为 `allowed_exts: []`（代码逻辑 `len>0` 才校验，空即不限制——types.go 注释本就写明）；注释保留"如需限制再填写"
- `docs/config.yaml` 部署模板原缺整个 upload 段（只在运行 config.yaml 里加过），本次补齐与运行配置对齐（driver/domain/max_size/allowed_exts/local/aliyun/qcloud/s3）
- 后端已重启（admin_test，healthz 200）；tmpcheck 临时程序验证配置解析：`driver=local max_size=10 allowed_exts=0`，验证后删除
- MEMORY.md 超注入上限被截断 → 全文精简重写（128 行→~100 行，合并重复/删除陈旧）；顺带修正残留旧结论"上传模块未用 proto（环境无 protoc）"（见 MEMORY.md proto 段，2026-09-05 修正）

## 个人中心头像支持 .ico（双端，2026-09-05）
- 背景：后端 allowed_exts 已清空（默认不限类型），但头像前端本地校验白名单仍限 5 种图片 → .ico 被拦"仅支持上传 jpg/..."
- 改动：`CurrentAccount.vue` / react `CurrentAccount.tsx` 的 `IMAGE_EXTS` 加 `'ico'`；mime 校验放宽为 `!IMAGE_EXTS.includes(ext) || (file.type && !file.type.startsWith('image/'))`（type 为空但扩展名合法也放行，防部分环境 ico 无 mime）；错误提示与 tip/extra 文案改为动态 `IMAGE_EXTS.join('/')` 与显式加 /ico，避免再次不同步
- 双端 lint 0 错误

## 本地存储返回完整访问路径（2026-09-05）
- 需求：local driver 返回资源完整 URL 而非相对路径。代码 `Local.URL`→`joinURL` 本就支持（domain 非空即绝对地址），只是 `config.yaml` 的 `upload.domain` 为空
- 改动：运行配置 `config.yaml` 与模板 `docs/config.yaml` 的 `upload.domain` 填 `http://127.0.0.1:3000`（与 server.port 一致，注释已说明），无需改 Go 代码（改配置后须重启 admin_test.exe）
- 历史数据回填：MySQL `UPDATE admin_upload SET url = CONCAT('http://127.0.0.1:3000', url) WHERE url LIKE '/upload/%'`，影响 2 行
- 提示：改端口/上线域名时须同步 domain；前端 vite proxy `/upload`→3000 仍保留（对绝对 URL 无影响）；config-dev.yaml 无 upload 段（-env dev 时 upload 走默认值，注意 domain 为空）

## 权限配置面板名称固定（双端 PermissionsSaveModal，2026-09-05）
- 需求：菜单「权限配置」面板里权限名称不可手工修改、跟随动作类型固定。用户明确**不做历史数据统一标准化**（库里"查看账号/重置密码/绑定角色"等语义名保留原样）
- 改动：名称输入框改**只读**（vue `:value`+`readonly` / react `readOnly`，placeholder 改"跟随动作类型自动生成"）；`onTypeChange` 无条件重算 `name = 菜单名 + 类型中文`（原只有 auto 行联动，现所有行含历史行切 type 都重算）；key 的 auto/manual 逻辑不变（手改 key 后 auto=false 不再随 type）；面板提示文案加"权限名称由「菜单名 + 动作类型」自动生成且不可修改"
- 双端 lint 0 错误；未改后端/DB/mock（纯 UI 行为）
