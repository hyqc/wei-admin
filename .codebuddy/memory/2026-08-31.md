# 2026-08-31

## 账号管理列表页调整（admin-vue3x/src/views/admin/user/index.vue）
1. **昵称列**：超过 10 字符用 `displayNickname()` 截断显示 `...`，鼠标悬停用 `a-tooltip` 显示完整昵称；列宽调至 160
2. **登录 IP 拆两列**：原 `lastLoginIp` 列拆为"上次登录IP / 当前登录IP"两列，`getLoginIp()` 按逗号拆分（last=倒数第2个、current=最后一个，空则显示 `-`）；`scroll.x` 调至 1880
- 数据源不变：mock `lastLoginIp` 为逗号分隔字符串（如 `'127.0.0.1, 127.0.0.1'`），SQL 为 JSON 数组（差异已记录在 08-28）

## 页面验证（playwright-cli）
- dev 服务 `npm run dev`（端口 8000）后台启动，账号 admin/123456 登录
- 验证通过：表头出现「上次登录IP」「当前登录IP」两列；admin 行两列均 127.0.0.1，无 IP 用户显示 `-`
- 昵称截断：临时将 mock test00001 昵称改为 12 字符，页面显示「测试00001测试账...」，hover 后 Tooltip 显示完整昵称「测试00001测试账号昵称」；验证后已恢复原数据
- 经验：playwright-cli 在 PowerShell 下 `run-code` 传内联 JS 会被引号拆分，改用 `--filename` 写脚本文件执行；`waitForSelector('.ant-table-tbody tr')` 会匹配不可见测量行超时，改用 waitForTimeout

## 菜单管理默认全展开（admin-vue3x/src/views/admin/menu/index.vue）
- `default-expand-all-rows` 在数据异步加载时不生效（初始化时 dataSource 为空），改为受控 `:expanded-row-keys` + `@update:expanded-row-keys`
- 新增 `collectParentKeys()` 递归收集所有含 children 的菜单 id，`getRows()` 成功后赋给 `expandedRowKeys`，实现每次加载默认全展开，用户仍可手动折叠
- playwright 验证：8 条菜单全部显示，系统设置/菜单管理展开图标为 expanded 状态 ✓

## 权限管理增加「所属菜单」列
- `index.vue` columns 增加 `{ title:'所属菜单', dataIndex:'menuName', key:'menuName', width:140 }`（在权限名称后），`scroll.x` 1000→1100，空值显示 `-`
- 顺带修复 3 个既有 bug（否则新列永远为空）：
  1. `AddPermissionModal` 未回写 `formState.menuId`（PageMenus 传的是 `:value` 非 `v-model`，emit('update:value') 被丢弃）→ `onMenuChange` 手动赋值，并提交 `menuName/menuPath`（`ReqAdminPermissionAdd` 已加这两个字段）
  2. `PageMenus` 首次打开弹窗时子组件未挂载，父组件 `pageMenusRef.load()` 拿不到实例 → 改为组件内 `onMounted(load)`
  3. `PageMenus` 的 a-tree：`default-expand-all` 对异步数据无效（只渲染折叠的根节点），且 `info.node.data` 在 antd-vue 中不存在导致拿不到菜单 → 改为受控 `:expanded-keys` + 数据加载后 `collectParentKeys` 全展开；`onSelect` 改为用 `keys[0]` 从 `menuMap`（id→MenuTreeItem）取数据；树加 `:height="320"` 让虚拟列表有确定高度
- 验证：新建权限选「接口管理」→ 列表显示 所属菜单=接口管理、唯一键=AdminApiView ✓（测试数据留在 mock 内存，重启 dev 即恢复）

## 5 个管理页操作按钮加图标
- 图标映射（@ant-design/icons-vue）：详情=EyeOutlined、编辑=EditOutlined、删除=DeleteOutlined、重置密码=KeyOutlined、绑定角色=UserSwitchOutlined、绑定权限/权限配置=SafetyCertificateOutlined、绑定接口=ApiOutlined、新增子菜单=PlusOutlined
- 操作列宽 +60 并同步 scroll.x：user 360→420(x1880→1940)、role 260→320(x1260→1320)、menu 320→380(x1100→1160)、permission 260→320(x1100→1160)、api 200→260(x1000→1060)
- 验证：5 页全部按钮均带图标（role 临时新建角色验证了编辑/绑定权限/删除），无列宽溢出（clientWidth==scrollWidth），临时角色已删除
## 前后端接口差异梳理（admin-vue3x → admin-backend）
- 环境：go1.24.4 + protoc-gen-go v1.36.11 有；**protoc 不可用**（grpc-tools 的 protoc.exe 缺失）→ 无法用 `make proto` 重新生成 pb.go，需装 buf（go install github.com/bufbuild/buf/cmd/buf@latest）或 protoc 才能改 .proto
- 后端 DB 配置 `config.yaml` 的 store.wei 已是 root/123456@127.0.0.1:3306/wei，符合要求
- 路由表在 `app/router/admin.go`；契约源在 `proto/admin_proto/*.proto`（pb.go 为生成物）
- **缺失接口 3 个**（前端有、后端无路由）：`/admin/menu/all`、`/admin/permission/all`、`/admin/role/permissions`
- **响应结构差异（全局）**：前端要 `{list, pageInfo:{total,pageNum,pageSize}}`，后端 proto 是 `{total,pageNum,pageSize,list}` 平铺
- **详情响应**：前端要对象本身，后端是 `RespXxxInfoData{ Xxx data = 1 }` 多一层 data 包裹
- **请求参数差异**：user/add 前端传 roleIds（后端无）且后端强校验 confirmPassword/enabled/avatar；user/edit 后端强校验 username/enabled；user/edit_pwd 后端要 confirmPassword；role/delete 后端要 permissionIds(required) 前端不传；menu/show 后端要 field(required) 前端不传；menu/add 字段名前端 isHideInMenu/isEnabled 后端 hideInMenu/enabled；permission/add 后端要 id(required) 前端新增无 id；permission/enable 字段 enabled vs 后端 isEnabled；permission/add_menu_permissions 前端传数组、后端要 {menuId, permissions}；role/all 后端 Resp 定义为空不返回列表；api/all 后端返回 {list} 前端要数组；menu/pages 同理；user/info 后端响应类型错用 AdminMenuModel

## 后端接口同步第一批：user + role（已完成）
- **工具链**：protoc 不可用 → 改用 buf（go install buf@v1.72.0，自动带 go1.26.7）+ protoc-go-inject-tag。在 `proto/admin_proto/` 建 `buf.yaml`（v2, modules: [path: .]）与 `buf.gen.yaml`（v2, plugin local protoc-gen-go, out: ., opt paths=source_relative）。生成命令：`buf generate` 然后 `protoc-go-inject-tag -input="*.pb.go"`（两步骤缺一不可，tag 由后者注入）。buf 1.72 已无 `buf protoc` 子命令
- 验证等价性：生成后 `go build ./...` 通过即视为可用（版本风格差异不影响业务代码）
- **通用差异处理模式**：前端分页参数平铺（pageNum/pageSize 顶层），后端原为 `Base` 嵌套 → 做法：proto 新增 `ReqFrontXxxList`（平铺）与 `RespFrontXxxListData{list, pageInfo}`，controller 用 Front 结构接收后映射成内部结构传给 logic（logic/dao 零改动），响应再转回前端结构。common.proto 新增 `PageInfo{pageSize, pageNum, total}`
- **user 模块**：proto 加 ReqFrontAdminUserList/RespFrontAdminUserListData/RespFrontAdminUserInfoData；Add 去 required 并加 `roleIds=8`、logic 默认 `IsEnabled:true` 且创建后绑定角色；Edit 只更新昵称邮箱（原会清空 username/禁用账号）；EditPassword 去掉 confirmPassword 强校验；Info 由 AccountInfo 转成账号信息+roleIds
- **role 模块**：proto 加 ReqFrontAdminRoleList/RespFrontAdminRoleListData；Delete 去掉 permissionIds 必填；Add 默认启用、Edit 不更新启用状态；补注册缺失路由 `/admin/role/permissions`（controller 方法已有），并把响应包成 `{list}`（复用 RespAdminRolePermissionsData）
- **验证**：go build 通过；启动 admin_test.exe 端口 3000 路由含 /admin/role/permissions；登录 DB 查询与 bcrypt 密码校验均通过（说明 wei 库连通、admin/123456 有效）
- **遗留（与本次改动无关的既有问题）**：登录在生成 JWT 时报 `登录令牌检查失败 token is malformed: token contains an invalid number of segments`（pkg/utils/jwt + getAccountInfo.createToken），疑似 nacos 远程配置（192.168.9.212）或 jwt RSA 配置问题，需单独排查

## 前端切到真实后端（mock 启动方式保留）
- `.env` / `.env.development` 的 `VITE_USE_MOCK` 改为 false；新增 `.env.mock`（true）+ package.json 脚本 `dev:mock: vite --mode mock`，需要 mock 时执行 `npm run dev:mock`
- **vite proxy 必须加 rewrite**：后端路由无 `/api` 前缀，前端 baseURL 是 `/api`，配置 `rewrite: (path) => path.replace(/^\/api/, '')`，否则全部 404
- **后端既有 bug（已修）**：`global/jwt.go` 构建 IgnoresMap 时用配置里的 `post`（小写）当 key，而中间件用 `ctx.Request.Method`（`POST`）查询 → 登录被鉴权拦截报 token malformed；已改为 `strings.ToUpper`。另 `middleware/auth.go` 的 `/xxx/*` 通配符分支体是空的（匹配到也不放行），已补 `ctx.Next(); return`
- **登录响应契约**：前端 `login` 的类型是 `post<AdminInfo>`，即 `res.data` 直接是 AdminInfo（扁平，含 token/expire/menus/permissions），不是 `{token,expire,data}`。后端须 `result.SetData(data.Data)` 去掉 data 包裹（`/admin/account/info` 同理）
- **expire 语义**：前端 `SetLoginToken` 存 `expire * 1000`（毫秒）并与 now 比较 → 后端必须返回**过期时间的秒级时间戳**（`token.ExpiryAt.Unix()`），返回有效期秒数（如 3600）会被判定为立即过期
- 联调验证（前端 8000 + 后端 3000）：登录跳 /home、token 已存；账号管理 12 列数据来自 wei 库（test555/test00004/test00003/test00002/admin）；角色管理返回数据库中的角色；无 console 错误
- 数据库实际数据与 wei.sql 不完全一致（库中有 test555、角色"水电费"等）

## 菜单管理空白问题（menu 模块部分同步）
- 根因：`/admin/menu/tree` 后端返回**带虚拟「顶级菜单」节点的数组**，前端期望 `{list: MenuTreeItem[]}` 且 list 应为 parentId=0 的菜单 → `res.data.list` 为 undefined 导致表格空
- 修复：proto 新增 `RespFrontMenuTreeData{list}`；controller.Tree 取虚拟根节点的 children 组装 list（`logic.Tree` 返回 `common.GetMenuTreeWithTop(data)`）
- 顺带修复：Info 去 data 包裹；Add 字段名改与前端一致（`isHideInMenu`/`isHideChildrenInMenu`/`isEnabled`，原为 hideInMenu/enabled，logic.Add 同步）；Edit 的 describe 去掉 required；Show 去掉 field 必填
- 验证：tree 返回 list=1(系统设置) + 5 个子菜单；前端菜单管理显示 6 行、无 console 错误
- **menu 模块剩余未同步**：`/menu/pages` 后端返回 {list} 前端要数组；`/menu/permissions` 后端字段 menuInfo 前端要 menu；`/menu/all` 后端无路由（前端有调用）；modes 已匹配

## 剩余模块全量同步完成（menu/api/permission/account）
- **menu 剩余**：`RespAdminMenuPermissionsData` 字段 `menuInfo`→`menu`（logic 同步）；Pages 改为返回数组 `data.List`；新增 `MenuController.All` + 路由 `/admin/menu/all`
- **api 模块**：新增 ReqFrontAdminApiList/RespFrontAdminApiListData，List 平铺+{list,pageInfo}；Add 默认启用（前端不传 enabled）、Edit 不再把 IsEnabled 置 false；Add/Edit 的 describe 去 required。Info/All 原本就返回前端期望结构（logic 返回对象/数组），无需改
- **permission 模块**：新增 ReqFrontAdminPermissionList/RespFrontAdminPermissionListData（平铺+pageInfo）、RespFrontAdminPermissionInfoData（权限信息+apiIds，controller 手工赋值）；Add 的 id 去 required（新增无 id）并加 menuName/menuPath、describe 去 required；Enable 字段 `isEnabled`→`enabled`（logic 同步）；AddMenuPermissions 改为 `ctx.ShouldBindJSON(&[]*ReqAdminPermissionAdd)` 接收数组后组装 BindMenu
- **account 剩余**：ReqAccountPasswordEdit 字段 `password`→`newPassword`（eqfield 改 NewPassword，logic 同步）；新增 `AccountController.Logout` + 路由 `/admin/account/logout`
- **未做**：`/admin/permission/all`、`/admin/menu/all` 前端虽定义但**实际未调用**（views 中无引用），故此前未新增；menu/all 已顺手补上
- 验证：编译通过；接口层 pages 为数组(6)、menu/permissions 返回 menu=账号管理+3 权限、permission/list total=15 含 menuName、api/list total=41、logout 正常；前端 5 个页面全部有数据且无 console 错误（menu 6 行 / permission 10 行 / api 10 行 / user 5 行 / role 2 行）

## 唯一键冲突返回对应错误码（新增数据已存在）
- 原因：`logic.AdminUser.Add` 直接 `return err`（原始 gorm 错误），未像 api/permission 模块那样捕获唯一键；且错误码里**根本没有"账号已存在"**
- 做法（后续新增错误码均按此流程）：① `proto/code_proto/error_code.proto` 加枚举 ② 在 `proto/code_proto/` 用 buf 重新生成（该目录另建了 buf.yaml/buf.gen.yaml）③ `code/code_zh.go` + `code/code_en.go` 加消息映射 ④ logic 里 `strings.Contains(err.Error(), "uk_xxx")` 捕获
- 新增错误码：`AdminAccountNameExist=400004`(uk_username)、`AdminMenuNameExist=700002`/`AdminMenuKeyExist=700003`/`AdminMenuPathExist=700004`(uk_name/uk_key/uk_path)、`AdminRoleNameExist=800002`(uk_name)
- **角色特殊**：实际库的 admin_role 可能没有 uk_name 索引（同名新增会成功）→ 在 `logic.AdminRole.Add` 增加重名预检（用 `dao.H.AdminRole.FindAllValid` 比对名称），与唯一键捕获双保险
- 验证：user/add=400004 账号已存在、role/add=800002 角色名称已存在、menu/add=700003 菜单键名已存在；前端新增重复 admin 弹窗提示"账号已存在"且弹窗保持打开；测试产生的重复角色已删除
- 注意：PowerShell 内联中文会乱码，测试脚本应写成 .ps1 文件，且重复数据名从接口动态获取避免手写中文

## 弹窗输入框被浏览器自动填充（新增账号显示 admin 及其密码）
- 现象：刷新后点新建账号，账号/密码框被填入登录保存的 admin 凭据。原因是浏览器密码管理器自动填充（DOM 被填但 Vue state 仍为空，resetFields 也拦不住）
- 修复：在含密码的输入框加 `autocomplete` 属性（antd 组件属性会透传到原生 input）：
  - `AddUserModal.vue`：a-form 加 `autocomplete="off"`；账号 `autocomplete="off"`；密码 `autocomplete="new-password"`
  - `ResetPasswordModal.vue`（重置他人密码，绝对不能填入管理员自己的密码）：同上，密码/确认密码用 new-password
  - `account/components/CurrentPassword.vue`：原密码 `current-password`（填充自己的原密码是合理帮助）、新密码/确认密码 `new-password`
  - **登录页保持自动填充**，不改
- 验证：两个弹窗 form 的 autocomplete=off、账号框 off、密码框 new-password、值均为空

## 超管账号（ID=1）标识与权限规则
- 后端原有能力已具备：`constant.AdministerId = 1`、`IsAdministrator()`；`getMyAdminPermissions` 对超管返回全部权限；`dao.IsAdminCanAccessPath` 对超管直接放行 → **超管自动拥有所有权限后端本就实现**
- 新增标识字段：`AdminUserListItem.isSuperAdmin`(14)、`RespFrontAdminUserInfoData.isSuperAdmin`(15)，logic `HandleItemData` 用 `constant.IsAdministrator(item.ID)` 赋值；controller Info 同步设置
- 新增错误码 `AdminSuperAccountNotAllow = 400005`（超管账号不允许该操作），`logic.BindRoles` 开头拦截 adminId==1
- 前端：types/common.ts 加 isSuperAdmin；账号管理角色列对超管显示金色「超级管理员」标签（替代角色标签）；「绑定角色」按钮 `v-if="!record.isSuperAdmin"`
- 编辑/重置密码对超管仍保留（改昵称邮箱、改密码不影响权限体系）
- 验证：列表仅 adminId=1 为 isSuperAdmin=true；bind_roles 超管返回 400005；前端 admin 行显示「超级管理员」且操作按钮只有详情/编辑/重置密码

## 超管角色（角色 ID=1）标识与可改描述
- 常量区分：`constant.AdministerId=1`（超管账号ID）与新增 `constant.AdministerRoleId=1`（超管角色ID）+ `IsAdministratorRole()`，避免账号/角色语义混用
- 后端：`RoleItem.isSuperAdmin`(9)、`RespAdminRoleInfoData.isSuperAdmin`(13)；logic `HandleItemData`/`Info` 用 `IsAdministratorRole` 赋值
- 后端保护：`logic.AdminRole.Edit` 中，超管角色**只更新 Describe，Name 保持不变**（即使前端/接口传入新名称也被忽略）
- 前端：types/admin_role.ts 加 isSuperAdmin；角色名称列对超管追加金色「超级管理员」标签；**编辑按钮去掉 `v-if="record.id !== 1"`**（超管可编辑）；绑定权限按钮仍仅非超管（超管自动拥有全部权限）
- EditRoleModal：超管角色时名称输入 `disabled` 并显示提示「超级管理员角色名称不可修改，仅可修改描述」，描述可编辑
- 验证：列表/详情 isSuperAdmin 正确；改超管名称被后端忽略（名称仍为「管理员」、描述更新成功）；前端名称禁用、描述可改、提示正确；测试后描述已复原为「普通管理员」

## 菜单父级选择：虚拟顶级菜单（ID=0）+ 修复「新增子菜单」变成编辑父菜单
- 背景：`/menu/tree` 返回的是 parentId=0 的菜单数组（已去掉虚拟根），导致菜单管理的父级下拉里没有"顶级菜单"可选项
- 方案（前端，后端无需改）：`SaveMenuModal` 的 `treeSelectData` 首项固定插入 `{ key:0, value:0, title:'顶级菜单' }`；新增时 `parentId = detailData?.parentId ?? 0`（默认顶级）。后端 Add 的 parentId `min=0`、Edit 会更新 ParentID，0 即顶级，后端本就支持
- **修复既有 bug**：`openAddModal(parent)` 原先把父菜单对象整体传给弹窗 `saveData`，而弹窗用 `detailData?.id` 判断编辑/新增 → 点"新增子菜单"实际打开的是**编辑父菜单**（标题显示"编辑菜单"、parentId 取父菜单的父级），保存会误改父菜单。改为只传 `{ parentId: parent.id }`，弹窗新增分支取 `detailData?.parentId`
- 验证：新建菜单→「新建菜单/顶级菜单」；系统设置下新增子菜单→「新建菜单/系统设置」；编辑系统设置→「编辑菜单/顶级菜单」；编辑账号管理→「编辑菜单/系统设置」；接口创建 parentId=0 的顶级菜单与系统设置平级，测试后已删除

## 菜单层级业务约定（用户已确认，保持现状不再改动）
- 规则：**实际业务菜单都是顶级分组菜单的子菜单，不与顶级菜单平级**。当前数据：系统设置(id=1,parentId=0) 为唯一顶级分组，5 个功能菜单（账号/角色/菜单/权限/接口管理）均 parentId=1
- 「顶级菜单（ID=0）」父级选项**保留**，新建菜单默认选中它（需要建平级的新分组时可用）；不做前端/后端强制校验，也不移除该选项
- 后端无需配合改：Add 的 parentId `min=0`、Edit 会更新 ParentID，0 即顶级，已支持

- 注意：antd 图标 DOM 是 `span.anticon` 包裹 svg，`svg.anticon` 选择器匹配不到

## 多页签（Tabs）功能实现
- 新增 `src/store/tabs.ts`：Pinia store，state `tabs: TabItem[]{path,title,closable}`，`watch(deep)` 写入 localStorage（key `admin_tabs`，常量 `LocalStorageTabsKey` 在 api/config.ts），初始化时从本地恢复
- 首页页签固定不可关闭（closable:false，HOME_TAB），恢复时过滤无效项并把首页置顶
- 新增 `src/components/TabsView.vue`：`a-tabs type=editable-card hide-add`，activeKey 绑定 `route.path`，watch 路由自动 `addTab`（标题取 `route.meta.title`，即菜单名）；每个 tab 标题用 `a-dropdown :trigger="['contextmenu']"` 包右键菜单（关闭当前/其他/全部，带图标）；`@edit` 处理 × 关闭；关闭当前后跳转相邻页签（`getNeighborPath`）
- `BasicLayout.vue` content 区 `<TabsView />` + `<router-view />`；`utils/common.ts` 的 `Logout()` 清除 tabs 缓存
- 未做 keep-alive：切换页签是路由跳转、组件重新渲染（不保留列表搜索状态）
- 验证：开新页签（首页→账号管理→角色管理）、重复点击不新增、刷新后 3 个页签保留且停留原路由、关闭其他/当前/×按钮/关闭全部均正确、首页无关闭按钮

## 去掉页面内菜单名称标题（避免与页签重复）
- `PageContainer.vue`：删除 `.header-title` 区块（`v-if="title"` + `#title` slot）、删除 `title` prop、删除 `.header-title` 样式；给 `.header-search` 加 `flex: 1`，保证无搜索区时 extra 按钮仍靠右
- 5 个管理页移除 `title="xxx"` 传值（user/role/menu/permission/api）
- 浏览器标签标题不受影响（仍取 route.meta.title）
- 验证：5 页均无 .header-title、header 文本不含页面名、操作按钮均靠右

## 权限配置弹窗：名称/键系统生成且不可改（menu/components/PermissionsSaveModal.vue）
- 需求：权限名称和键不能修改，由系统自动生成
- **关键发现：后端 `/admin/menu/permissions`（logic.GetMenuPermissions）在菜单无权限时已自动生成查看/编辑/删除三类**（`common.AdminPermissionEnumList(menuId, menuKey)`），即"系统生成"后端本就实现，前端拿到的 existing 永非空（name="查看"、key=菜单key+View 等）
- 前端改动：name/key 输入框 `disabled` + 提示文案「权限名称与唯一键由系统根据菜单自动生成，不允许手动修改」；existing 分支补充 `typeName`（后端 MenuPermissionItem 无该字段，按 type 用 DEFAULT_PERMISSION_TYPES 映射）；watch 内局部变量捕获 menuId/menuName/menuPath 避免异步回调取 props 异常（作为前端兜底生成）
- 验证：新菜单权限配置显示 查看/编辑/删除、key=PermTestView 等、均 disabled、保存成功、测试数据已清理

## 绑定接口弹窗按所属菜单分组（permission/components/BindApisModal.vue）
- 需求：绑定接口里接口列表太乱，按权限所属菜单分组展示选择
- **方案（纯前端，后端零改动）**：admin_api 表无菜单字段，接口所属菜单通过「admin_permission_api→admin_permission.menu_id」推导；弹窗打开时 `getAdminApiAll()` + `getAdminPermissionList({pageSize:10000})`（后端 pageSize 无上限）→ 按接口 id 反查归属菜单（多权限绑定时取 menuId 最小者），未绑定归「未分组」，按 menuId 排序分组
- 渲染：`a-collapse` 分组（默认全展开），header=`菜单名（接口数）`，extra 放「全选」checkbox（`@click.stop` 防折叠），组内 `a-checkbox-group` 复选
- 验证：账号查看权限弹窗按 账号管理(7)/角色管理(10)/菜单管理(10)/权限管理(7)/接口管理(7) 分组；已绑定 3 个接口正确勾选；组全选 3→8→0 正确；数据未改动

## 绑定接口弹窗改为树形（最终方案，替换上面的 collapse 分组）
- 用户反馈 collapse 分组"还是很乱"，要求参考角色管理绑定权限（BindPermissionsModal）的 a-tree 方式
- **演进**：collapse 分组 → 三级树（菜单→权限→接口）→ **两级树（菜单→接口，最终）**。三级树接口多对多导致接口重复出现（如 adminUser::add 挂在查看/编辑两权限下），父权限节点半选状态混乱 → 改两级树，接口按归属菜单分组且**全局唯一**
- 实现：`a-tree` checkable，`:height=480` 虚拟滚动，默认展开一级；节点 key `menu-{menuId}` / `api-{apiId}`（接口唯一）；`onCheck` 直接存 checkedKeys，保存时 filter `api-` 前缀解析 apiId 去重；回显时递归收集 `api-` 节点中 apiId∈当前权限绑定集
- 验证：账号查看弹窗 账号管理(7 接口)/角色管理…/ 分组、接口不重复、3 个已绑定接口回显勾选、菜单节点半选联动、点击勾选/取消正常、未保存改动数据

## 权限管理"启用无效"修复（字段名错用 enabled→isEnabled）
- **根因**：后端 proto `PermissionListItem.isEnabled`（列表项），前端 `permission/index.vue` 及编辑/详情组件误用 `record.enabled`（恒 undefined）→ 开关恒显示"禁用"、点击恒发 `enabled:true`，表现为"启用无效"
- 修复：`types/admin_permission.ts` 的 `PermissionListItem.enabled`→`isEnabled`；`permission/index.vue` 4 处 `record.enabled`→`record.isEnabled`（popconfirm 文案、RowEnabledButton、删除按钮 v-if、updateEnabled 传参）；`EditPermissionModal.vue`/`DetailPermissionDrawer.vue` 的 `detailData.enabled`→`detailData.isEnabled`
- 字段对照（proto）：列表项=**isEnabled**；Add/Edit/Enable 请求体=**enabled**；详情 AdminPermissionInfo=**isEnabled**；菜单 MenuTreeItem=**enabled**（菜单页用法正确勿改）；角色列表=**isEnabled**
- 验证：开关与后端一致全显示启用；enable 接口调用 + 刷新回显正确；测试中误禁用了 id=1 已恢复启用（数据无残留改动）

## 账号管理登录 IP 两列无值修复
- **根因**：后端列表接口 `HandleItemData` 用 `utils.BeanCopy`（`pkg/utils/utils.go`）把 model.AdminUser 复制到 proto.AdminUserListItem。BeanCopy 按**字段名反射匹配**（`FieldByName`），model 字段 `LastLoginIP` 与 proto 字段 `LastLoginIp` 大小写不同 → 登录 IP 永远复制不上 → 列表返回 `lastLoginIp` 恒空（JSON omitempty 省略）→ 前端"上次/当前登录IP"显示 '-'。详情/登录接口（getAccountInfo）手动赋值所以正常
- 修复：后端 `admin_user.go` `HandleItemData` 手动 `data.LastLoginIp = handleListLoginIp(item.LastLoginIP)`（新增函数：JSON 数组字符串 `["ip1","ip2"]` → 逗号分隔字符串）；前端 `user/index.vue` `getLoginIp` 增强（优先 JSON.parse，失败按逗号拆分），兼容后端返回与 mock 格式
- 存储约定：数据库 `last_login_ip` 为 JSON 数组字符串（`SetAdminUserLastLoginIp` 追加，最多保留 2 个）；列表返回逗号分隔字符串与 mock 一致；`last` 取倒数第 2、`current` 取最后 1 个，只有 1 次登录时 last 显示 '-'
- 后端 dev 进程是 `admin-backend/admin_test.exe`（端口 3000，经 vite 代理 8000）；改 Go 代码后需 `Stop-Process` + `go build -o admin_test.exe .` + `Start-Process` 重启生效
- 验证：admin 行两列显示 127.0.0.1，登录次数递增；未登录用户显示 '-'
