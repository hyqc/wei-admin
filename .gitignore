# Binaries for programs and plugins
_long-time-test/long-time-test
*.exe
*.exe~
*.dll
*.so
*.dylib
*swp
*~
.idea

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
cover.cov

# Dependency directories (remove the comment below to include it)
# vendor/
../.idea
*.exe
*.log

# Node 依赖与构建产物（各前端目录下的 node_modules / dist 一并忽略）
node_modules/
dist/
dist-ssr/
*.local
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
